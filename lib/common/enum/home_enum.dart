enum HeaderPopupMenu {
  addDir,
  editDir,
  batchDelDir,
  delDir,
  moveDir,
  moveRootDir
}

enum HomeTitleBarNavTabPage { welcome, link, note, img, video,audio,other }

enum LinkDataListRowOperationPopupMenu {
  openBrowser,
  del,
  edit,
  move,
  copyLink,
  detail
}

enum ImgDataListRowOperationPopupMenu { del, rename, move, download, open,copyPath }

enum VideoDataListRowOperationPopupMenu { del, rename, move, download, open }

enum AudioDataListRowOperationPopupMenu { del, rename, move, download, open }

enum NoteDataListRowOperationPopupMenu { del, rename, move, download, open,edit}

enum FileDataUploadState { notUploaded, uploadSuccess, uploadedFail }

enum NoteFileSpecialTreatmentKey { excel,pdf}

enum OtherDataListRowOperationPopupMenu { del, rename, move, download, open }

