import 'package:flutter/material.dart';

class DialogIconStyleType {
  final double size;
  final Color color;
  final Color hoverColor;
  final Color enterColor;

  const DialogIconStyleType(
      {required this.size,
      required this.color,
      required this.hoverColor,
      required this.enterColor});
}
